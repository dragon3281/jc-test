# 自动化数据检测平台 - 开发指南

## 📖 项目已完成内容

### ✅ 基础架构 (100%)

#### 1. 项目结构
- 后端Maven项目完整结构
- 前端Vue3项目完整结构  
- Docker容器化配置
- 数据库表设计(9张表)

#### 2. 配置文件
- application.yml - 完整的Spring Boot配置
- pom.xml - Maven依赖配置
- package.json - NPM依赖配置
- docker-compose.yml - Docker服务编排
- vite.config.js - Vite构建配置

#### 3. 核心代码
**后端:**
- DetectionPlatformApplication.java - Spring Boot主类
- Server.java - 服务器实体类
- Result.java - 统一响应封装
- SystemConstants.java - 系统常量定义

**前端:**
- main.js - Vue应用入口
- App.vue - 根组件
- router/index.js - 路由配置(完整13个路由)
- utils/request.js - Axios封装

#### 4. 文档
- README.md - 项目说明(285行)
- 实施总结.md - 实施报告(310行)
- 项目清单.md - 文件清单(239行)
- 开发指南.md - 本文档

### 📊 代码统计

```
已创建文件总数: 20个
代码文件: 16个
文档文件: 4个
Java类: 4个
Vue/JS文件: 4个
配置文件: 8个
SQL脚本: 1个(216行)
```

## 🚀 快速开发指南

### 后端开发流程

#### Step 1: 创建实体类(Entity)
剩余需要创建的实体类:
```java
// 已完成
✅ Server.java

// 待创建
- ProxyPool.java (代理池)
- ProxyNode.java (代理节点)
- BaseData.java (基础数据)
- PostTemplate.java (POST模板)
- DetectionTask.java (检测任务)
- TaskServer.java (任务服务器关联)
- DetectionResult.java (检测结果)
- User.java (用户)
```

**创建模板:**
```java
@Data
@TableName("t_xxx")
public class XxxEntity implements Serializable {
    @TableId(type = IdType.AUTO)
    private Long id;
    
    // 其他字段
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;
    
    @TableLogic
    private Integer deleted;
}
```

#### Step 2: 创建DTO和VO

**DTO示例(请求参数):**
```java
@Data
public class ServerAddDTO {
    @NotBlank(message = "服务器名称不能为空")
    private String serverName;
    
    @NotBlank(message = "IP地址不能为空")
    private String ipAddress;
    
    // ... 其他字段
}
```

**VO示例(响应数据):**
```java
@Data
public class ServerVO {
    private Long id;
    private String serverName;
    private String ipAddress;
    private String statusText;
    // ... 其他字段
}
```

#### Step 3: 创建Dao层

```java
@Mapper
public interface ServerMapper extends BaseMapper<Server> {
    // MyBatis Plus提供基础CRUD
    // 自定义复杂查询
}
```

#### Step 4: 创建Service层

**接口:**
```java
public interface ServerService extends IService<Server> {
    Result<Void> addServer(ServerAddDTO dto);
    Result<ServerVO> getServerById(Long id);
    Result<PageVO<ServerVO>> pageServers(ServerQueryDTO dto);
}
```

**实现:**
```java
@Service
public class ServerServiceImpl extends ServiceImpl<ServerMapper, Server> 
        implements ServerService {
    // 实现业务逻辑
}
```

#### Step 5: 创建Controller层

```java
@RestController
@RequestMapping("/server")
public class ServerController {
    
    @Autowired
    private ServerService serverService;
    
    @PostMapping("/add")
    public Result<Void> add(@Valid @RequestBody ServerAddDTO dto) {
        return serverService.addServer(dto);
    }
}
```

### 前端开发流程

#### Step 1: 创建API接口

```javascript
// src/api/server.js
import request from '@/utils/request'

export function getServerList(params) {
  return request({
    url: '/server/list',
    method: 'get',
    params
  })
}

export function addServer(data) {
  return request({
    url: '/server/add',
    method: 'post',
    data
  })
}
```

#### Step 2: 创建页面组件

```vue
<!-- src/views/server/Index.vue -->
<template>
  <div class="server-container">
    <el-table :data="tableData" style="width: 100%">
      <el-table-column prop="serverName" label="服务器名称" />
      <el-table-column prop="ipAddress" label="IP地址" />
      <el-table-column prop="status" label="状态">
        <template #default="{ row }">
          <el-tag :type="getStatusType(row.status)">
            {{ getStatusText(row.status) }}
          </el-tag>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { getServerList } from '@/api/server'

const tableData = ref([])

const loadData = async () => {
  const res = await getServerList()
  tableData.value = res.data
}

onMounted(() => {
  loadData()
})
</script>
```

#### Step 3: 配置Pinia状态管理(可选)

```javascript
// src/stores/server.js
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useServerStore = defineStore('server', () => {
  const serverList = ref([])
  
  function setServerList(list) {
    serverList.value = list
  }
  
  return { serverList, setServerList }
})
```

## 📝 开发模块清单

### 后端模块开发顺序

1. **基础模块** (优先)
   - [ ] 全局异常处理
   - [ ] JWT工具类
   - [ ] AES加密工具类
   - [ ] MyBatis Plus自动填充配置
   - [ ] 跨域配置
   - [ ] 日志切面

2. **用户认证模块**
   - [ ] 用户实体和Mapper
   - [ ] 登录接口
   - [ ] Token刷新接口
   - [ ] 密码修改接口

3. **服务器管理模块**
   - [ ] SSH连接工具类
   - [ ] Docker客户端工具类
   - [ ] 服务器CRUD接口
   - [ ] 服务器监控接口
   - [ ] 容器管理接口

4. **代理资源池模块**
   - [ ] 代理池CRUD接口
   - [ ] 代理节点CRUD接口
   - [ ] 代理健康检查Service
   - [ ] 代理分配算法

5. **POST模板模块**
   - [ ] 模板CRUD接口
   - [ ] 模板测试接口
   - [ ] 模板版本管理

6. **数据中心模块**
   - [ ] Excel导入Service
   - [ ] 数据导出Service
   - [ ] 数据查询接口
   - [ ] 数据统计接口

7. **检测任务模块**
   - [ ] 任务创建接口
   - [ ] 任务拆分Service
   - [ ] 任务调度Service
   - [ ] RabbitMQ消费者
   - [ ] WebSocket推送

8. **定时任务**
   - [ ] 服务器健康检查定时任务
   - [ ] 代理健康检查定时任务
   - [ ] 任务状态同步定时任务

### 前端模块开发顺序

1. **基础组件** (优先)
   - [ ] 主布局Layout.vue
   - [ ] 登录页Login.vue
   - [ ] 404页面

2. **仪表盘**
   - [ ] Dashboard.vue
   - [ ] 系统概览卡片组件
   - [ ] ECharts图表组件

3. **服务器管理**
   - [ ] 服务器列表页
   - [ ] 添加服务器对话框
   - [ ] 服务器详情抽屉
   - [ ] 状态徽章组件

4. **代理资源池**
   - [ ] 代理池列表(树形)
   - [ ] 代理节点表格
   - [ ] 添加代理对话框
   - [ ] 健康度进度条组件

5. **数据中心**
   - [ ] 基础数据页
   - [ ] 最新数据页
   - [ ] 历史数据页
   - [ ] Excel导入组件
   - [ ] 数据导出组件

6. **业务中心**
   - [ ] 网站分析页
   - [ ] 自动化注册页
   - [ ] POST模板页
   - [ ] JSON编辑器组件

7. **检测任务**
   - [ ] 新建检测页(步骤表单)
   - [ ] 任务列表页
   - [ ] 已完成任务页
   - [ ] 任务进度条组件
   - [ ] 实时日志组件

## 🔧 开发工具推荐

### 后端开发
- **IDE**: IntelliJ IDEA
- **数据库工具**: Navicat / DBeaver
- **API测试**: Postman / Apifox
- **Redis客户端**: RedisInsight
- **SSH工具**: Termius

### 前端开发
- **IDE**: VS Code
- **插件**:
  - Volar (Vue3支持)
  - ESLint
  - Prettier
- **浏览器插件**: Vue DevTools

## 📦 部署流程

### 开发环境

1. **启动基础服务**
```bash
./start.sh
```

2. **启动后端**
```bash
cd backend
mvn spring-boot:run
```

3. **启动前端**
```bash
cd frontend
npm run dev
```

### 生产环境

1. **后端打包**
```bash
cd backend
mvn clean package -DskipTests
```

2. **前端打包**
```bash
cd frontend
npm run build
```

3. **Docker部署**
```bash
# 取消docker-compose.yml中backend和frontend的注释
cd docker
docker-compose up -d
```

## 🐛 调试技巧

### 后端调试
- 使用IDEA的Debug模式
- 查看控制台日志
- 使用MyBatis日志查看SQL
- Postman测试接口

### 前端调试
- 浏览器开发者工具
- Vue DevTools查看组件状态
- Network面板查看API请求
- Console查看日志

## 📚 参考文档

- [Spring Boot官方文档](https://spring.io/projects/spring-boot)
- [MyBatis Plus官方文档](https://baomidou.com/)
- [Vue 3官方文档](https://cn.vuejs.org/)
- [Element Plus官方文档](https://element-plus.org/)
- [Docker官方文档](https://docs.docker.com/)

## 🎯 下一步行动

根据系统设计文档,建议按以下顺序开发:

**Week 1-2: 基础模块**
- 完善所有实体类
- 实现全局异常处理
- 实现用户认证模块

**Week 3-4: 核心功能**
- 服务器管理模块
- 代理资源池模块

**Week 5-6: 业务功能**
- POST模板模块
- 数据中心模块

**Week 7-9: 任务调度**
- 任务管理模块
- 任务调度服务
- WebSocket实时推送

**Week 10-12: 前端开发**
- 所有页面组件
- 交互优化
- 样式美化

**Week 13-14: 测试与优化**
- 单元测试
- 集成测试
- 性能优化

---

**项目已完成基础架构搭建,可以开始模块化开发!**
